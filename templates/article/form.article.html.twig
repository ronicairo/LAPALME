{% extends "base.html.twig" %}

{% block title %}Admin - Ajouter un article
{% endblock %}

{% block body %}

{% if article is not defined %}
	<h1 class="text-center my-3">Ajouter un article</h1>
{% else %}
	<h1 class="text-center my-3">Modifier l'article N°{{ article.id }}</h1>
{% endif %}

	<div class="row">
		<div class="col-5 mx-auto">

			{{ form_start(form) }}

			{{ form_row(form.titre) }}
			{{ form_row(form.soustitre) }}
			{{ form_row(form.contenu) }}

			<div class="row">
				<div class="col6">{{ form_row(form.photo) }}</div>
			</div>

			{{ form_end(form) }}

		</div>
	</div>

{% endblock %}
